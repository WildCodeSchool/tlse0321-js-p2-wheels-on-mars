import { createMuiTheme } from '@material-ui/core';

const Theme = createMuiTheme({
  palette: {
    primary: {
      main: '#fff',
    },
    secondary: {
      main: '#941b0c',
      dark: '#bc3908',
      light: '#621708',
    },
    // type: "dark"
  },
});
export default Theme;
